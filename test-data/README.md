# 測試資料

所有落地的測試資料統一放在此目錄，完整規則見根目錄 `AGENTS.md`。

- `fixtures/`：可重複使用的固定測試素材，需要時建立並納入 Git。
- `runtime/`：自動生成且不納入 Git 的測試存檔、備份、截圖、日誌、報告及 Electron profile。

單元測試會清除自己建立的執行目錄；桌面 smoke test 保留其執行目錄供檢查，並在終端機印出位置。確認不再需要後，可刪除對應的執行子目錄。
